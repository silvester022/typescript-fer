(() => {

  let isSuperman:boolean = true
  let isBatman:boolean = true 

  // isBatman = true && true
  // isBatman = (isBatman) ? 'a' : 'b'
  isBatman = (isBatman) ? true : false
  
  // console.log({isBatman})
  console.log({isBatman})
})()